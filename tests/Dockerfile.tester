FROM debian:bookworm-slim
# DÜZELTME: JSON işlemek için 'jq' ekliyoruz.
RUN apt-get update && apt-get install -y curl time jq && rm -rf /var/lib/apt/lists/*
COPY ./test-runner.sh /test-runner.sh
RUN chmod +x /test-runner.sh