{
  "name": "web",
  "private": true,
  "version": "1.0.6",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "backend:start": "cargo run -p sentiric-companion",
    "tauri:dev": "wait-on http://localhost:8080 && cargo tauri dev -p sentiric-companion",
    "dev:companion": "npm-run-all --parallel backend:start tauri:dev"
  },
  "dependencies": {
    "@preact/signals": "^1.2.3",
    "@tauri-apps/api": "^1.5.6",
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "@tauri-apps/cli": "^1.6.0",
    "@types/node": "^24.6.0",
    "npm-run-all": "^4.1.5",
    "typescript": "~5.9.3",
    "vite": "^7.1.7",
    "wait-on": "^9.0.1"
  }
}
